---
- hosts: dummy10

  tasks:
    - setup:
      delegate_to: "{{item}}"
      delegate_facts: yes
      with_items: "{{groups['test']}}"

    - name: Add the name of each host to a file
      copy:
        dest: /tmp/dummyhosts.txt
        content: |
          {% for h in groups['test'] if not h == 'dummy10' %}
          {{ hostvars[h]['inventory_hostname'] ~ ' has ' ~
             hostvars[h]['ansible_default_ipv4']['address'] }}
          {% endfor %}
